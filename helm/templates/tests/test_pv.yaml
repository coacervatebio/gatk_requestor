apiVersion: v1

kind: PersistentVolume

metadata:
  name: coacervate-test-pv
  annotations:
    "helm.sh/hook": test

spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 5Gi
  hostPath:
    path: {{ .Values.test.assetPath }}