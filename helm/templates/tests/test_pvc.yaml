apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: coacervate-test-pvc
  annotations:
    "helm.sh/hook": test
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
  selector:
    matchLabels: {{ .Values.test.pvcLabel }}